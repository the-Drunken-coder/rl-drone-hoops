numpy>=1.23
gymnasium>=0.29

# MuJoCo wheels are version/Python dependent.
# For Python 3.9 on Linux, `mujoco==2.3.7` is a known-good pin. Newer Python versions
# typically require newer MuJoCo wheels, so keep the requirement flexible.
mujoco>=2.3.7

# Optional (training/logging; install when ready)
# torch>=2.0
# stable-baselines3>=2.2
# sb3-contrib>=2.2
# imageio>=2.34
# imageio-ffmpeg>=0.4
